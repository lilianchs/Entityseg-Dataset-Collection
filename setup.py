from setuptools import setup, find_packages

setup(
    name='Entityset-Dataset-Collection',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Your Name',
    description='Entity segmentation dataset tools and visualization',
)
